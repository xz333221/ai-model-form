{
  "name": "ai-model-form-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Demo project consuming the ai-model-form npm package (latest).",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:client": "vite",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "ai-model-form": "file:..",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "concurrently": "^9.1.0",
    "vite": "^7.0.0"
  }
}
