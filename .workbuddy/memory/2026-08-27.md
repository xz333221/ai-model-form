- 18:14 在 AddModelForm.vue / server/middleware.js / i18n 中加入 NVIDIA 供应商，使用官方绿色图标 `nvidia-color`；为 NVIDIA 端点补充内置模型列表。
- 18:22 用户反馈 NVIDIA 测试超时；确认模型下拉已返回实时模型，证明 API Key 有效。将服务端默认 testTimeoutMs 从 8s 调整为 30s 并重启 dev server。
- 18:34 用户测试返回 410；确认为 NVIDIA 账号缺少 Public API Endpoints 权限导致，非代码/Key 问题。优化 middleware.js 对 NVIDIA 410 的错误提示文案。
